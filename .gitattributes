# Force LF regardless of local autocrlf settings - these files run inside
# Linux containers and a CRLF shebang breaks exec (e.g. entrypoint.sh).
* text=auto eol=lf

*.sh text eol=lf
Dockerfile text eol=lf
*.png binary
*.jpg binary
*.ico binary
